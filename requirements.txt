# ARKITECT — Python dependencies
# Install with:  pip install -r requirements.txt
# (the START.bat launcher does this for you automatically on first run)

fastapi==0.136.3
uvicorn[standard]==0.49.0
httpx==0.28.1
pypdf==6.13.2
pywebview==6.2.1   # native desktop window (WebView2) so ARKITECT runs as a real app, not a browser tab
